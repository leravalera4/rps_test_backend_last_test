# 🔍 Проверка автоматического перечисления выигрыша на продакшене

## Проблема
После деплоя перестали перечисляться выигрышные SOL победителю.

## Возможные причины

### 1. ⚠️ Service Wallet не инициализирован

**Проверка:**
- В логах при старте сервера должно быть: `Auto-finalization service initialized`
- Должен быть адрес: `Service wallet: <адрес>`

**Решение:**
Добавьте переменную окружения на платформе деплоя:
```
SERVICE_WALLET_PRIVATE_KEY=[массив из 64 чисел]
```

Чтобы получить приватный ключ:
```bash
cd backend
node -e "const wallet = require('./src/config/serviceWallet').getServiceWallet(); console.log(JSON.stringify(Array.from(wallet.secretKey)))"
```

### 2. ⚠️ Service Wallet не имеет баланса

**Проверка:**
В логах должно быть:
```
Service wallet balance: X.XXX SOL
```

Если баланс < 0.01 SOL, нужно пополнить.

**Решение:**
1. Скопируйте адрес service wallet из логов
2. Отправьте devnet SOL на этот адрес (минимум 0.1 SOL)
3. Используйте faucet: https://faucet.solana.com

### 3. ⚠️ Неправильный RPC URL

**Проверка:**
В логах должно быть: `🔗 Solana RPC URL: <url>`

**Решение:**
Убедитесь, что на платформе деплоя установлена переменная:
```
SOLANA_RPC_URL=https://api.devnet.solana.com
```

Или для mainnet:
```
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
```

### 4. ⚠️ Ошибки при отправке транзакций

**Проверка:**
В логах при завершении игры должно быть:
```
🚀 ===== STARTING AUTO-FINALIZATION =====
📋 Game ID: <gameId>
🏆 Winner: <winner>
💰 Service wallet balance: X.XXX SOL
📤 Sending finalization transaction...
✅ Game finalized successfully!
```

Если есть ошибки, они будут в логах с префиксом `❌`.

## 🔧 Диагностика

### Проверьте логи на платформе деплоя:

1. **При старте сервера:**
   - Ищете: `Auto-finalization service initialized`
   - Ищете: `Service wallet: <адрес>`
   - Ищете: `Service wallet balance: X.XXX SOL`

2. **При завершении игры:**
   - Ищете: `🚀 ===== STARTING AUTO-FINALIZATION =====`
   - Ищете: `✅ Game finalized successfully!` или `❌ Transaction failed:`

### Типичные ошибки:

1. **"Service wallet not initialized"**
   → Установите `SERVICE_WALLET_PRIVATE_KEY`

2. **"insufficient balance"**
   → Пополните service wallet

3. **"RPC connection issue"**
   → Проверьте `SOLANA_RPC_URL`

4. **"Game account not found on-chain"**
   → Игра не была создана через smart contract

## 📝 Что было исправлено

1. ✅ Добавлена проверка баланса service wallet перед отправкой транзакции
2. ✅ Добавлена автоматическая инициализация service wallet если он не инициализирован
3. ✅ Улучшено логирование ошибок с указанием конкретных проблем
4. ✅ Добавлена поддержка `SOLANA_RPC_URL` из переменных окружения
5. ✅ Добавлено детальное логирование транзакций

## 🚀 Быстрое решение

1. Убедитесь, что на платформе деплоя установлены переменные:
   ```
   SERVICE_WALLET_PRIVATE_KEY=<ваш приватный ключ>
   SOLANA_RPC_URL=https://api.devnet.solana.com
   ```

2. Пополните service wallet:
   - Получите адрес из логов при старте
   - Отправьте минимум 0.1 SOL на этот адрес

3. Перезапустите сервер

4. Проверьте логи при завершении игры - должны быть детальные сообщения о процессе

